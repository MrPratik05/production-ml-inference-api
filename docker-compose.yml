services:
  ml-api:
    build: .
    container_name: production-ml-inference-api
    ports:
      - "8000:8000"
    environment:
      - MODEL_NAME=distilbert-base-uncased-finetuned-sst-2-english
      - LOG_LEVEL=INFO
